# kubernetes-vagrant-lab
This repository provides a Vagrant-based lab environment to create virtual machines suitable for experimenting with Kubernetes and cloud-native tools.

At this stage, the repository focuses only on provisioning virtual machines. Kubernetes installation and configuration will be added incrementally in future iterations.

## Requirements

- Vagrant
- VirtualBox
Usage

```bash
vagrant up
```


